# PyPI 最小发布练习实验报告

## 一、实验目的

掌握 Python 包发布到 TestPyPI 的完整流程，包括包结构创建、构建、上传及验证安装。

## 二、实验环境

- 操作系统：Windows 11 Pro
- Python 版本：Python 3.13
- 构建工具：setuptools, build
- 上传工具：twine 6.2.0
- 目标平台：TestPyPI (https://test.pypi.org/)

## 三、实验步骤

### 3.1 创建项目结构

```
mypackage_demo/
├── pyproject.toml          # 项目配置文件
├── README.md               # 项目说明文档
└── src/
    └── mypackage_demo/
        └── __init__.py     # 包源代码
```

### 3.2 编写配置文件

**pyproject.toml:**

```toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackage_demo_20260626"
version = "0.0.1"
authors = [
    {name = "Your Name", email = "your.email@example.com"}
]
description = "A simple demo package for PyPI practice"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[tool.setuptools.packages.find]
where = ["src"]
```

### 3.3 编写源代码

**src/mypackage_demo/__init__.py:**

```python
def hello():
    """Return a greeting message."""
    return "Hello from mypackage_demo!"
```

### 3.4 构建包

```bash
py -m pip install --upgrade build
py -m build
```

构建成功后生成以下文件：

```
dist/
├── mypackage_demo_20260626-0.0.1-py3-none-any.whl
└── mypackage_demo_20260626-0.0.1.tar.gz
```

### 3.5 上传到 TestPyPI

```bash
py -m pip install twine
py -m twine upload --repository testpypi --username __token__ --password <API_TOKEN> dist/*
```

上传成功后访问地址：
https://test.pypi.org/project/mypackage-demo-20260626/0.0.1/

### 3.6 验证安装

```bash
py -m pip install --index-url https://test.pypi.org/simple/ mypackage_demo_20260626
py -c "from mypackage_demo import hello; print(hello())"
```

输出结果：
```
Hello from mypackage_demo!
```

## 四、实验结果

| 步骤 | 状态 |
|------|------|
| 创建包结构 | 成功 |
| 编写配置文件 | 成功 |
| 构建包 | 成功 |
| 上传到 TestPyPI | 成功 |
| 远程安装验证 | 成功 |

## 五、注意事项

1. **包名唯一性**：PyPI/TestPyPI 上的包名必须唯一，建议使用 `包名_日期` 格式避免冲突
2. **双因素认证**：TestPyPI 要求开启双因素认证才能创建 API Token
3. **API Token**：上传时用户名使用 `__token__`，密码使用 API Token（以 `pypi-` 开头）
4. **src 布局**：使用 `src/` 目录存放源代码是推荐的做法，需要在 `pyproject.toml` 中配置 `where = ["src"]`

## 六、实验总结

通过本次实验，成功完成了 Python 包从创建到发布的完整流程。掌握了以下技能：

1. 标准的 Python 包目录结构设计
2. `pyproject.toml` 配置文件的编写
3. 使用 `build` 模块构建 wheel 和 sdist 分发包
4. 使用 `twine` 上传包到 TestPyPI
5. 从 TestPyPI 安装并验证包的功能

---
实验日期：2026年6月26日
